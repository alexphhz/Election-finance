# Data Guide

- Place **raw** source files in `data/raw/` (not committed by default).
- Save intermediate/cleaned files to `data/processed/` for quick repro.
- Document source URLs and any manual steps here (ex: Census, BEA, FEC, OpenSecrets, FHFA, SBA, Kaggle).
